# Copy this file to ".env" and change the values if you want.
# The real .env file is ignored by git so passwords are never committed.
# These are only used for the local database.

POSTGRES_USER=promatch
POSTGRES_PASSWORD=changeme
POSTGRES_DB=promatch
